# Traefik Cloudflare DNS API token (scoped: DNS Edit on zones used for ACME).
# Prefer cloudflare_acme_dns_token; fall back to cloudflare_api_token for legacy single-token setups.
CF_DNS_API_TOKEN={{ cloudflare_acme_dns_token | default(cloudflare_api_token) }}
