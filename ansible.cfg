[defaults]
# Resolve vault secrets when group_vars contain ansible-vault ciphertext.
vault_password_file = ~/keys/ansible/.ansible-vault-kaiju
# Optional: so you can run from the repo root without passing -i every time:
#   ansible-playbook ansible/playbooks/preflight.yml
inventory = ansible/inventory/hosts.yml

